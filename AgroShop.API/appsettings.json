{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Provide the real value through environment variables or user secrets in production.
    "DefaultConnection": ""
  },
  "JwtSettings": {
    "Issuer": "AgroShop",
    "Audience": "AgroShop",
    // Provide the signing key through environment variables or user secrets in production.
    "SecretKey": "",
    "AccessTokenExpirationMinutes": 15,
    "RefreshTokenExpirationDays": 7
  }
}
